import Crossword from '../../src/modules/Crossword';

describe('Crossword', () => {
  describe('.generateEmptyGrid', () => {
    beforeEach(() => {
      emptyArray = Crossword.generateEmptyGrid(4);
    })

    xit('creates a square array of arrays, where the contents are empty string', () => {

    })
  })
})
